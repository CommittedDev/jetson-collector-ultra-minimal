# Ultra-Minimal Collector Configuration
# Copy this file to .env and edit the values

# ============================================
# REQUIRED - Must be set before running
# ============================================

# Server URL for uploading captures
SERVER_URL=https://your-capture-server.com

# API key for authentication
API_KEY=your-secure-api-key-here
# ============================================
# OPTIONAL - Defaults shown
# ============================================

# Device identifier (default: hostname)
DEVICE_ID=

# Capture interval in seconds
CAPTURE_INTERVAL_SECONDS=10

# Number of captures to batch before syncing
BATCH_SIZE=20

# Frames to capture for sharpness selection
BURST_FRAMES=5

# Minimum Laplacian variance for sharpness (warning below)
# Normalized by resolution - 250 works well for 4K cameras
SHARPNESS_THRESHOLD=250.0

# Maximum local storage in GB
MAX_STORAGE_GB=100.0

SYNC_IDLE_INTERVAL_SECONDS=60
SYNC_CATCHUP_DELAY_SECONDS=2

# Minimum cameras required to start
MIN_CAMERAS=2

# Storage path (relative to working directory)
STORAGE_PATH=./data/captures

# Days to retain synced images before deletion (0=keep forever)
RETENTION_DAYS=3

# ============================================
# CRONITOR HEARTBEAT (Optional)
# Leave empty to disable heartbeat monitoring
# ============================================

# Cronitor API key (from cronitor.io dashboard)
CRONITOR_API_KEY=

# Cronitor monitor key (the unique ID for your heartbeat monitor)
CRONITOR_MONITOR_KEY=

# Heartbeat interval in seconds
HEARTBEAT_INTERVAL_SECONDS=60

# ============================================
# NOTE: No camera device configuration needed!
# Cameras are auto-discovered at startup.
# Just plug them into any USB port.
# ============================================
